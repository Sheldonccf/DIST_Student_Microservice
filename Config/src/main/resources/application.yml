server:
  port: 8888
spring:
  application:
    name: config-single-server
  cloud:
    config:
      discovery:
        enabled: true
      server:
        encrypt:
          enabled: false
        git:
          uri: https://github.com/Sheldonccf/DIST_Student_Microservice #配置文件所在仓库
          username: Sheldonccf
          password: Ccf080018@
          default-label: master #配置文件分支
          search-paths: Shared  #配置文件所在根目录
